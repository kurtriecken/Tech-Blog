

<style>
    /* Styling for the input */
    .custom-input {
        background-color: #f7f7f7;
        border: 2px solid #BA9B37;
        border-radius: 5px;
        color: #333;
        font-size: 16px;
        padding: 10px;
        width: 300px;
        outline: none;
    }

    /* Hover effect */
    .custom-input:hover {
        border-color: #1e72e5;
    }

    /* Focus effect */
    .custom-input:focus {
        border-color: #1e72e5;
        box-shadow: 0 0 5px rgba(30, 114, 229, 0.7);
    }
</style>
<title>The Tech Blog</title>
</head>

<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/profile">Profile</a></li>
                <li><a style="cursor: pointer;" id="logout">Logout</a></li>
            </ul>
        </nav>
    </header>
    <div class="logo">
        <h1>The Tech Blog</h1>
    </div>
    <main>
        <h1>Welcome, {{username}}!</h1>
        <div id="profile-post-list">
            {{#each blog_posts as |blog_post| }}
            <div style="width: 100%">
                <div class="card card-bp">
                    {{!-- <a id="bp-num-{{blog_post.id}}" href="/"></a> --}}
                    <div class="title">
                        <h2 id="title-{{blog_post.id}}">{{blog_post.title}}</h2>
                    </div>
                    <div class="content">
                        <p id="content-{{blog_post.id}}">{{blog_post.content}}</p>
                    </div>
                    <div class="username">{{username}}</div>
                </div>
                <button data-id="{{blog_post.id}}" class="delete-button">&#128465;</button>
                <button data-id="{{blog_post.id}}" style="background-color: green"
                    class="edit-button fa">&#xf040;</button>
            </div>
            {{/each}}
        </div>

        <br><button id="new-post-btn">Create a new post!</button>
    </main>
    {{!--
    <script src="./js/login.js"></script> --}}
    <script src="/js/logout.js"></script>
    <script src="/js/navnewpost.js"></script>
    <script src="/js/deletepost.js"></script>
</body>